<?xml version="1.0" encoding="utf-8"?>
<!--
  Développement uniquement (source set `debug`) : autorise le HTTP en clair vers le poste
  qui héberge l'API. 10.0.2.2 est l'alias de la machine hôte vu depuis l'émulateur Android.
  La compilation `release` n'embarque PAS ce fichier : la production reste en HTTPS strict.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
